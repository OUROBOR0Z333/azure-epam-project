# Ansible Inventory for Frontend Demo VM
# This file will be dynamically updated with the VM's IP address after deployment

[frontend]
# The frontend VM will be added here after deployment
# Format: vm-frontend-demo ansible_host=<PRIVATE_IP> ansible_user=azureuser ansible_ssh_private_key_file=~/.ssh/id_rsa

[all:vars]
ansible_python_interpreter=/usr/bin/python3